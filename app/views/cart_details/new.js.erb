$('#result').html(
	"<% if @cart_detail.errors.any? %><% else %>追加しました!　ただいま、<%= @cart_detail.num %>個購入しました！<% end %> "
	);

$('#has_error').html(
	"<% if @cart_detail.errors.any? %>99個までしか選択できません<% end %> "
	);